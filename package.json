{
  "name": "zexl-audio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Small hosted URL-to-audio converter for MP3, FLAC and WAV.",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js",
    "check": "node --check src/server.js && node --check src/converter.js && node --check src/innertube.js && node --check src/extractor-errors.js && node --check src/validation.js"
  },
  "engines": {
    "node": ">=24.21.0 <25"
  },
  "author": "void"
}
